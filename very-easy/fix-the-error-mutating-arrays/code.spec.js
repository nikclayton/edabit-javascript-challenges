const minusOne = require('./code');

describe('Tests', () => {
    test('the tests', () => {
        expect(minusOne(x)).toEqual([1, 2, 3, 4]);
        expect(minusOne(x)).toEqual([1, 2, 3, 4]);
        expect(minusOne(x)).toEqual([1, 2, 3, 4]);
        expect(minusOne(x)).toEqual([1, 2, 3, 4]);
        expect(minusOne(x)).toEqual([1, 2, 3, 4]);
    });
});
